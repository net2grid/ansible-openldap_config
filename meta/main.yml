---
galaxy_info:
  author: Remco Brink
  description: Configures an LDAP server by creating groups and users
  license: license (BSD, MIT)
  min_ansible_version: 2.0
  platforms:
  - name: Debian
    versions:
    - all
  - name: Ubuntu
    versions:
    - all
  galaxy_tags:
    - ldap

dependencies:
  - net2grid.openldap_server
